---
description: Best practices for Vue 3 development
globs: **/*.{vue,ts,js}
---
- Use Composition API for better code organization and reusability
- Implement proper reactivity with `ref` and `reactive`
- Use `<script setup>` for concise component definitions
- Leverage Vue 3's improved TypeScript support